# WinPeak Admin
NEXT_PUBLIC_BASE_URL=http://localhost:3000
AUTH_URL=http://localhost:3000
AUTH_SECRET=replace-with-a-long-random-string

# Same PostgreSQL database as the public site
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/winpeak

# Admin sign-in (staff only, separate from player accounts)
ADMIN_EMAIL=admin@winpeak.local
ADMIN_PASSWORD=WinPeakAdmin123!

# Public site origin used in affiliate tracking links
WINPEAK_SITE_URL=http://localhost:3000
